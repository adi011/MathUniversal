<Page
    x:Class="MathUniversal.ExpressionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MathUniversal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
     DataContext="{Binding Source={StaticResource Locator}, Path=Expressions}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView ItemTemplate="{StaticResource ExpressionTemplate}" ItemsSource="{Binding Expressions}"/>    
        <Button HorizontalAlignment="Center" VerticalAlignment="Bottom" Content="Calculate" Command="{Binding CalculateCommand}" />
        <Button HorizontalAlignment="Left" VerticalAlignment="Bottom" Content="Add" Margin="10,0,0,0" Command="{Binding AddCommand}" />
        <Button Content="Temporary back navigate" Margin="164,493,0,115" Click="BackToStartPage" />
    </Grid>
</Page>
