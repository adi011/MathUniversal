<Page
    x:Class="MathUniversal.TheSystemOfEquationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MathUniversal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding Source={StaticResource Locator}, Path=TheSystemOfEquations}"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Background="#3F53ff">
            <ListView Margin="42,10,112,354">
                <TextBox Foreground="#8B97FF" Background="#464C7F" Width="145"  />
                <TextBox Foreground="#8B97FF" Background="#464C7F" Width="145"/>
            </ListView>
            <Button Content="TemporaryBackNavigate" Foreground="#8B97FF" Background="#464C7F" Command="{Binding BackToStartCommand}" Margin="42,577,0,31" Width="125"/>
            <Button Margin="74,523,0,85" Foreground="#8B97FF" Background="#464C7F">Add</Button>
        </Grid>
    </Grid>
</Page>
